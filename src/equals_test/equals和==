Java程序中测试两个变量 是否相等有两种方式。一是利用==运算符。另一种利用equals方法。
如果两个变量时基本类型，且都是数值（不一定要求数据类型严格相同）则只要两个变量相等，使用==判断将放回true。
两个引用变量，必须他们指向同一个对象时，==判断才会返回false。

String已经重写了Object的equals（）方法。，String的equals方法判断两个字符串相等的标准是：只要两个字符串包换的字符序列相同，通过equals比较将返回
true，否则返回false。